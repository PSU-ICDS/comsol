# Export the module function if it exists
[[ $(type -t module) == "function" ]] && export -f module

# Make SLURM_NNODES environment variable available to check nodecount for batchlicense
SLURM_EXPORT_ENV=SLURM_NNODES